import type { PageServerLoad } from './$types';

export const load = (async () => {
    
    // Fetch API Data
    // Firebase admin SDK
    // access.env
    // use Raw Sql
    // Access file system
    
    return {
        title: "My data"
    };
}) satisfies PageServerLoad;